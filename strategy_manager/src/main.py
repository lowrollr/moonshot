import socket
import os
import threading
import sys
import time
from server import startServer
from client import startClient

if __name__ == "__main__":
    time.sleep(50)
    # startServer()
    startClient()